import React from "react";
import styles from "./components/footer/footer.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <span>Develop by (STS)</span>
    </footer>
  );
};

export default Footer;
